# Dashboard SLA — dérivé de /tableau-de-bord

`SyntheseServeur.slaContractuel` et `uptime30j` viennent du backend (`/tableau-de-bord`), pas de texte statique vitrine.
Vérifier que `offres/tarifs` tuiles "Disponibilité engagée" == `synthese.slaContractuel`.

Ne pas inventer de SLA sans source mesurée (voir PLAN-UX §4).

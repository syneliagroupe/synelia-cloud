# Zimbra relayhost déjà fait (d1786ef) — MX inbound

Pas de MX public (127.0.0.1:25 only). Outbound via `zimbra.smile.ci:587` → AgentMail SPF. Inbound non requis lab.

Voir `docs/runbook/zimbra-relay-persist.md`.
